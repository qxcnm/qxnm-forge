# shadcn/ui 生成组件

本目录由 `shadcn` CLI 生成，作为项目的 UI primitive 基线。不要手工修改这些文件；视觉定制放在业务组件、`src/index.css` 与 Tailwind token 中。

重新生成组件时使用固定的 [`components.json`](../../../components.json)，并在变更后执行类型检查、测试和视觉 QA。
